# HUMANS.md — everything only a human can do

Each item: what, exact step, and where it's needed. Status reflects 2026-07-07 evening.

## 1. Accounts (all exist ✅ unless noted)

| Service | Status | Needed for |
| --- | --- | --- |
| Butterbase (dashboard.butterbase.ai) | ✅ app `app_gsuwgmmbc74g` live | Auth, persistence, credits, AI gateway |
| RocketRide Cloud (rocketride.ai) | ✅ key in `.env`, wss connected | Diagnosis pipeline |
| Nebius Token Factory (tokenfactory.nebius.com) | ✅ key in `.env` | Runbook embeddings (GraphRAG) |
| Daytona (app.daytona.io) | ✅ key in `.env` | Sandbox fix verification |
| GitHub | ✅ classic PAT in `.env`, write-verified | PR shipping — PAT needs **Contents + Pull-requests: write** on `GITHUB_REPO` |
| Neo4j Aura (console.neo4j.io) | ✅ instance `f4d17b7d` | Code graph + runbook vector index |
| Stripe (via Butterbase billing) | ❌ **NOT configured** | Real subscription→credits grant (see §3.1) |
| Opsera | ❌ no account | M6 Phase 2 governance telemetry (see §4) |

## 2. Secrets — every env var, what it is, where to get it

Copy `.env.example` → `.env` and fill (the template now lists everything; blank = required):

| Var | What / where to get it |
| --- | --- |
| `ROCKETRIDE_URI` + `ROCKETRIDE_APIKEY` | `https://api.rocketride.ai` + key from the RocketRide dashboard |
| `BUTTERBASE_APP_ID` | dashboard.butterbase.ai → your app (currently `app_gsuwgmmbc74g`) |
| `BUTTERBASE_API_URL` / `BUTTERBASE_GATEWAY_URL` | `https://api.butterbase.ai` / `…/v1` |
| `BUTTERBASE_API_KEY` | App service key (bb_sk_…) from the dashboard — also authenticates the AI gateway |
| `BUTTERBASE_CHAT_MODEL` | e.g. `anthropic/claude-sonnet-4.6` |
| `NEBIUS_BASE_URL` / `NEBIUS_API_KEY` / `NEBIUS_EMBED_MODEL` | tokenfactory.nebius.com → API keys; confirm the embed model ID on their models page (IDs change; `scripts/phase0-smoke` records the vector dimension) |
| `DAYTONA_API_KEY` (+ optional `DAYTONA_API_URL`) | app.daytona.io → settings → API keys |
| `GITHUB_TOKEN` / `GITHUB_REPO` | github.com → Settings → Developer settings → classic PAT (Contents + PRs write); repo currently `Eman-Gon/pager-zero-demo` |
| `NEO4J_URI` / `NEO4J_USERNAME` / `NEO4J_PASSWORD` / `NEO4J_DATABASE` | Aura console → instance credentials (download at create time) |
| `SERVICE_EMAIL` / `SERVICE_PASSWORD` | The demo on-call Butterbase user. **No default in code anymore — must be set in `.env`** |
| `DEMO_AUTO_CREDITS=1`, `UNLIMITED_CREDITS=0` | Correct demo-day values (paywall real, credits granted without Stripe) |
| `RESCUEOPS_AGENT_PIPELINE=0` | Stays 0 natively (agent pipe needs a Neo4j MCP on :8787 we don't run) |
| `OPSERA_WEBHOOK_URL` / `OPSERA_API_KEY` / `OPSERA_PIPELINE_ID` | Leave unset until an Opsera account exists (§4) |

Frontend (`frontend/.env.local`, auto-generated by `dev-native.sh`): `VITE_SENSOR_URL`/`VITE_RESPONDER_URL`
**must be absolute URLs on any hosted deploy** — the `/sensor`+`/responder` defaults only work through the
Vite dev proxy.

## 3. Manual setup still owed by a human

1. **Create a Stripe test-mode plan in Butterbase billing** — the app currently has **zero plans**
   (`GET /billing/plans → []`), so the real subscription→credits grant (`ensureAccount`) is dead code in
   production terms and the Credits panel's "Subscribe" cannot complete. Dashboard → app → Billing →
   create a plan in **test mode**; then subscribe once as the demo user and confirm `apply_credits += 5`.
   Until then the demo runs on `DEMO_AUTO_CREDITS=1` (honest, persisted, but not Stripe).
2. **Rotate the demo account password after the event** — `Resc!ue0ps2026` shipped in git history and in
   the live JS bundle. It's a throwaway demo account; rotate it at dashboard → auth users when done.
   (Removed from all committed files now; `dev-native.sh` reads it from `.env`.)
3. **Never build a hosted frontend with `VITE_NEO4J_*` set** — the Aura password lands in the public JS.
   The current live deploy is clean (verified); the local `frontend/dist` build is NOT — rebuild with
   those vars blank before any redeploy. Long-term fix (post-event): proxy graph reads via the responder.
4. **Finish (or revert) the claimflow patient migration** — the running stack targets
   `patients/claimflow` + `Eman-Gon/pager-zero-demo`, but `break.sh`/`reset.sh`/`e2e.sh`/
   `e2e-negative.sh`/`DEMO.md` still assume `target-repo`/`computeTax`. Decide the demo patient, update
   the scripts/docs to match, then **re-run `./scripts/e2e.sh` and `./scripts/e2e-negative.sh` end-to-end
   green before demo day**. This is the #1 open blocker.
5. **Butterbase MCP account mismatch (dashboard ops)** — the Claude Code Butterbase MCP is logged into
   ranjiv's account; `app_gsuwgmmbc74g` lives under emanpiece's. Dashboard-level changes (billing plan,
   auth users, access_mode) must be done by emanpiece or after re-authing the MCP (`/mcp` → logout →
   login as the app-owning account). Schema/data-plane work needs no dashboard (service key works).
6. **Push the patient repo to GitHub** — `GITHUB_REPO=Eman-Gon/pager-zero-demo` must exist and contain
   the patient's baseline so PR diffs render; verify `preflight.sh` passes its GitHub check.
7. **Optional hardening** — set the app's access mode to `authenticated` (anon currently gets 200 `[]`
   instead of 401; RLS already hides all rows, so this is defense-in-depth, not a leak fix).

## 4. Open decisions

| Decision | Options | Consequence |
| --- | --- | --- |
| **Demo patient: claimflow or target-repo?** | claimflow (richer story, migration in flight) vs target-repo (fully rehearsed, scripts/docs already match) | Undecided = broken verification suite. If claimflow: update break/reset/e2e/DEMO.md and re-verify; if target-repo: restart stack without `PATIENT_REPO` and re-verify |
| **Opsera: in or out?** | Get an account + set `OPSERA_WEBHOOK_URL`, or present the local policy gate only | Code side is done (deterministic gate enforced pre-PR either way; webhook is telemetry-only and silently off when unset). Out = don't claim the Opsera integration on stage |
| **Twilio: in or out?** | Not built | Out unless someone builds it — nothing in the repo references it |
| **Stripe on stage?** | Live test-mode subscribe (needs §3.1) vs `DEMO_AUTO_CREDITS` narration | Without §3.1 the Subscribe button is a dead end — don't click it on stage |
